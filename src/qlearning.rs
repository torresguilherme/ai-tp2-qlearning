enum sides
{
    UP,
    RIGHT,
    DOWN,
    LEFT
}

pub fn generateRandomPos(map: &Vec<Vec<char>>, posX: &usize, posY: &usize)
{
}

pub fn learn(map: &Vec<Vec<char>>, qValues: &Vec<Vec<Vec<f32>>>,
    learnRate: &f32, discount: &f32, posX: &usize, posY: &usize)
{
}
